#include <iostream>

class Complex {
public:
	double real;
	double image;
	
	Complex(){};
	Complex(double re, double im) : real(re), image(im) {};
};

int main() {
	/*Complex a;
	Complex b = { 1, 3 };
	Complex c(5, 4);*/

	return 0;
}